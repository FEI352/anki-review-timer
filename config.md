## Configuration Options

- **timer_duration_seconds**: How long the countdown timer runs (default: 30)
- **auto_show_answer**: Whether to automatically reveal the answer when time runs out (default: true)
- **show_pause_indicator**: Show the pause icon prefix when the timer is paused (default: true)
